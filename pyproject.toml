[tool.pytest.ini_options]
testpaths = ["tests/backend"]
addopts = "-x -q"

[tool.ruff]
line-length = 100
target-version = "py311"
src = ["backend", "synthetic"]

[tool.ruff.lint]
select = ["E", "W", "F", "I", "UP"]
ignore = []

[tool.coverage.run]
source = ["backend"]
omit = ["tests/*", ".venv/*"]
